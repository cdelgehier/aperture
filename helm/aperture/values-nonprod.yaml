replicaCount: 1

aperture:
  serviceName: aperture-nonprod
  logLevel: debug
  cacheBackend: memory

auth:
  mode: "off"

resources:
  requests:
    cpu: 50m
    memory: 128Mi
  limits:
    memory: 256Mi
